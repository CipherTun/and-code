# AGENTS.md

## Worktree

At the start of every new session, ALWAYS create a git worktree before making any changes:

1. Create a new branch and worktree: `git worktree add ../opencode-android-<branch-name> -b <branch-name>`
2. Work exclusively within the worktree directory.
3. Never modify the main working tree directly.
